specVersion: "v1beta"
serviceId: "aiot"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "aiot-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
